{
  "name": "jungleoats",
  "version": "1.0.0",
  "description": "",
  "private": "true",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf build",
    "start": "node index.js",
    "pug": "pug src/templates/views --out build -O '{ pretty: false }'",
    "build": "npm run pug",
    "dev": "npm run start && npm run pug:watch",
    "deploy": "npm run build && surge -p build -d jungleoats.surge.sh"
  },
  "author": "Lance Guyatt <lance@lanceguyatt.com>",
  "license": "ISC",
  "dependencies": {
    "serve": "^5.0.3"
  },
  "devDependencies": {
    "node-fs-extra": "^0.8.1",
    "pug": "^2.0.0-beta11",
    "rimraf": "^2.6.1"
  }
}
